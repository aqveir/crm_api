version: '3.9'

services:
  aqveir-api:
    stdin_open: true
    tty: true
    privileged: true
    container_name: aqveir-api
    image: aqveir-api
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "8000:8989"

networks:
  default:
    name: aqveir-api-network
    external: true
